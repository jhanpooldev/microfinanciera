name: microfinanciera
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.0 
  cloud_firestore: ^5.4.0
  uuid: ^4.5.1
  fl_chart: ^0.68.0
  intl: ^0.18.1
  flutter_secure_storage: ^8.0.0
  firebase_storage: ^12.4.10
  image_picker: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
